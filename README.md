Helper methods to dump the full coredump of a golang application.

Using the awesome [go-delve](https://github.com/go-delve/delve) but produces one searchable text output instead of an interactive shell.

